interface:
  display_name: "Execution Orchestrator"
  short_description: "Route multi-issue execution and emit a resumable handoff when blocked"
  default_prompt: "Use $codex-webui-execution-orchestrator to delegate intake, choose one current Issue target, keep routing toward the requested end-state, and create a resumable .tmp handoff if the run must stop blocked."
